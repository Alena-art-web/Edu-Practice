// Напишите функцию, которая преобразует шестнадцатеричное число (заданное в виде строки) в десятичное число.

//  Пример кода:
//    hexToDec('1') => 1
//    hexToDec('a') => 10
//    hexToDec('10') => 16
//    hexToDec('FF') => 255
//    hexToDec('-C') => 12

function toNumber(n) {
    let number = parseInt(n, 16);
}

toNumber('-c');